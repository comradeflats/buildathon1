/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // This must match your repository name
  basePath: '/buildathon',
  assetPrefix: '/buildathon',
};

module.exports = nextConfig;
